




<div class="row">
  <div class="col-md-12">
    <h1 class="text-center"><%= @tag.phrase %></h1>
  </div>
</div>

  <% @tag.tweets.order(created_at: :desc).each do |tweet| %>
  <div class="row">
    <div class="col-md-12">
      <div class="well">
        <p>
	      <%= tweet.user.name %> <%= "@#{tweet.user.username}" %>
	      • <%= time_ago_in_words(tweet.created_at) %>
        </p>
	      <p>
	      <%= tweet.message.html_safe %>
	      </p>
     </div>
  </div>
</div>
<% end %>
